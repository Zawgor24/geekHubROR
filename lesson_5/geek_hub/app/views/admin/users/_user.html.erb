<tr>
  <td><%= user.id %></td>
  <td><%= user.email %></td>
  <td><%= user.admin %></td>
  <td><%= link_to 'Show', admin_user_path(user) %></td>
  <td><%= link_to 'Update', edit_admin_user_path(user) %></td>
  <td><%= link_to 'Delete', admin_user_path(user),
                            method: :delete,
                            data: {confirm: 'Are you Sure?'} %></td>
</tr>