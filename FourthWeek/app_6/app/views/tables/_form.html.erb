<%= form_for @table do |f| %>
  <% if @table.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@table.errors.count, "error") %> prohibited
        this table from being saved:
      </h2>
      <ul>
        <% @table.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :color %>
    <%= f.text_field :color %>
  </p>

  <p>
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </p>

  <p><%= f.submit %></p>
<% end %>