<%= form_for @sportsman do |f| %>
  <% if @sportsman.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@sportsman.errors.count, "error") %> prohibited
        this sportsman from being saved:
      </h2>
      <ul>
        <% @sportsman.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :sport %>
    <%= f.text_field :sport %>
  </p>

  <p>
    <%= f.label :rank %>
    <%= f.number_field :rank %>
  </p>

  <p><%= f.submit %></p>
<% end %>