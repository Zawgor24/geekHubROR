<tr>
  <td><%= cat.name %></td>
  <td><%= cat.age %></td>
  <td><%= link_to 'Show', cat %></td>
  <td><%= link_to 'Edit', edit_cat_path(cat) %></td>
  <td><%= link_to 'Delete', cat,
                  method: :delete,
                  data: {confirm: 'Are you sure'} %></td>
</tr>